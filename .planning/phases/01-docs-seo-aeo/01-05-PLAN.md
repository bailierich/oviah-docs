---
phase: 01-docs-seo-aeo
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - docs/AUTHOR-GUIDELINES.md
  - content/help/account/billing-and-plans.mdx
  - content/help/account/notifications.mdx
  - content/help/account/privacy-and-security.mdx
  - content/help/account/updating-billing-info.mdx
  - content/help/bookings/block-time-off.mdx
  - content/help/bookings/client-reminders.mdx
  - content/help/bookings/managing-your-calendar.mdx
  - content/help/bookings/no-show-protection.mdx
  - content/help/clients/client-profiles.mdx
  - content/help/clients/importing-clients.mdx
  - content/help/clients/notes-and-history.mdx
  - content/help/getting-started/account-setup.mdx
  - content/help/getting-started/adding-services.mdx
  - content/help/getting-started/personalizing-your-profile.mdx
  - content/help/getting-started/your-booking-link.mdx
  - content/help/payments/connecting-oviah-pay.mdx
  - content/help/payments/deposits.mdx
  - content/help/payments/payouts.mdx
  - content/help/payments/refunds.mdx
  - content/help/profile/adding-a-profile-photo.mdx
  - content/help/profile/brand-colors-and-fonts.mdx
  - content/help/profile/personalizing-your-booking-page.mdx
autonomous: false
requirements:
  - SC-7  # Answer-shaped intros + meta descriptions
  # Also covers: D-08 (H1 + slug verification), D-13 (link anchor guidelines), D-14 (manual index submission)

must_haves:
  truths:
    - "An author-facing guideline document exists at docs/AUTHOR-GUIDELINES.md covering H1 enforcement, slug rules, intro patterns, meta description patterns, and link anchor patterns"
    - "Every article's `frontmatter.description` follows the answer-shaped pattern (direct answer with concrete UI path, no marketing filler)"
    - "Every article's first body paragraph after the first <h2> heading is an answer-shaped direct answer (not a 'In this guide we will...' filler intro)"
    - "Every article has exactly one rendered <h1> on the page (currently delivered by ArticlePage.jsx from frontmatter.title)"
    - "User has manually submitted sitemap.xml to Google Search Console and Bing Webmaster Tools post-deploy"
  artifacts:
    - path: "docs/AUTHOR-GUIDELINES.md"
      provides: "Author guideline doc covering D-08, D-11, D-12, D-13"
      contains: "Answer-Shaped"
  key_links:
    - from: "docs/AUTHOR-GUIDELINES.md"
      to: "content/help/**/*.mdx"
      via: "documented patterns enforced by review during authoring"
      pattern: "answer-shaped|direct answer|UI path"
---

<objective>
Land the answer-shaped content patterns and final post-deploy index submissions.

Purpose:
- Google's AI Overviews and Perplexity's answer cards extract the FIRST clear sentence after a heading. If that sentence is a marketing preamble ("In this guide..."), the AI cites a competitor. If it's a direct answer with a concrete UI path ("Go to **Calendar > Availability** and select..."), the AI cites OVIAH.
- The same applies to `<meta description>` — Google increasingly rewrites descriptions, but a direct-answer description anchors the rewrite.
- An author guideline doc (D-08 + D-11 + D-12 + D-13) prevents regression: any new article will follow the same patterns.
- Manual submission to Google Search Console and Bing Webmaster Tools (D-14) is the final step that gets the new sitemap into the indexes.

Output:
- `docs/AUTHOR-GUIDELINES.md` — author-facing rules covering H1, slugs, intro patterns, meta descriptions, internal link anchors. Documented, not enforced by code (no lint rule — that's scope creep for this phase).
- 22 MDX files — `description` and first-body-paragraph rewrites where needed.
- A human-verify checkpoint confirming the rewrite pass landed correctly.
- A human-action checkpoint for the manual Google Search Console + Bing Webmaster Tools submission (the only TRULY-manual step in this phase — the dashboards have no API for first-time sitemap submission without OAuth setup, which is itself out of scope).

Depends on Plan 01 because the rewrites modify the same MDX files that Plan 01 normalized. Running this in Wave 3 ensures Plan 01's mechanical sweep is done first, so this plan's editorial pass is the only frontmatter change in flight.
</objective>

<discretionary_resolutions>
1. **`#` → `h1` mapping verification** (CONTEXT discretion D-08): The planner verified that ZERO MDX files in `content/help/**` contain a `# ` line (`grep '^# ' content/help` returned no matches). Articles instead use raw `<h2 className="section-heading">` JSX inside MDX, and the page's actual `<h1>` is rendered in `components/ArticlePage.jsx` line ~93 from `frontmatter.title`. RESOLUTION: The "every article has exactly one `# H1`" rule from the spec is ALREADY SATISFIED via the JSX-rendered `<h1 className="article-title">{frontmatter.title}</h1>` in `ArticlePage.jsx`. We do NOT need to add `# H1` markdown to MDX files — that would create TWO H1s per page. Document this in the author guidelines: "the H1 comes from `frontmatter.title` automatically; do not add a `# Heading` line to the MDX body."

2. **Slug enforcement** (CONTEXT discretion): No CI lint rule. Author guideline only. The 22 existing slugs are already descriptive (`connecting-oviah-pay`, `block-time-off`, etc) — no rewrites needed. A future plan could add an ESLint custom rule or a build-time check; out of scope for this phase.

3. **Frontmatter validation** (CONTEXT discretion): Resolved in Plan 01 — no Zod, no schema. Trust the mechanical sweep + manual review. This plan's review checkpoint catches anything that slipped.

4. **Scope of intro/description rewrites** (CONTEXT discretion D-11): There are 22 articles. The planner spot-checked `connecting-oviah-pay.mdx` and `account-setup.mdx`:
   - `connecting-oviah-pay.mdx` description: `"Link your Stripe or Square account to start collecting deposits and getting paid."` — borderline. Direct enough but lacks a UI path. RECOMMEND lightweight rewrite: `"To connect Stripe or Square to OVIAH, go to Settings > Payments and click Connect on your provider."`
   - `connecting-oviah-pay.mdx` first body paragraph: `"OVIAH Pay is the built-in payment system that lets you collect deposits..."` — this is a definition, not an answer. RECOMMEND the first body paragraph become an answer to the implied question "How do I connect a payment provider?".
   - `account-setup.mdx` description: `"Everything you need before your first client booking — services, profile, and payments."` — marketing-shaped. NOT answer-shaped. REWRITE.
   
   Given 22 articles and an estimated 2-5 minutes per article for editorial review + rewrite, the FULL pass is ~1-2 hours. Spec estimates 1-2 hours. This is feasible in one task. Do all 22.

5. **Where to put the guideline doc**: `docs/AUTHOR-GUIDELINES.md` lives in the same `docs/` folder as `PHASE-docs-seo-aeo.md`. Rationale: discoverable next to the spec, not buried under `.planning/`.

6. **Index submission as a checkpoint, not a code task** (D-14): Google Search Console and Bing Webmaster Tools both require human-driven OAuth + property verification + sitemap upload through their dashboards. There is no public API for first-time sitemap submission without already-verified ownership. This is one of the rare LEGITIMATE `checkpoint:human-action` cases — Claude cannot do it via CLI/API. The checkpoint is BLOCKING for the phase to be considered "done" but does NOT block the rest of this plan's tasks.
</discretionary_resolutions>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-docs-seo-aeo/01-CONTEXT.md
@.planning/phases/01-docs-seo-aeo/01-01-SUMMARY.md
@docs/PHASE-docs-seo-aeo.md
@AGENTS.md
@components/ArticlePage.jsx
@content/help/payments/connecting-oviah-pay.mdx
@content/help/getting-started/account-setup.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/AUTHOR-GUIDELINES.md</name>
  <files>docs/AUTHOR-GUIDELINES.md</files>
  <read_first>
    - docs/PHASE-docs-seo-aeo.md (Sections "MDX Content Rules", "Write Article Intros as Direct Answers", "Meta Descriptions — Answer-Shaped", "Internal Linking — Question-Matched Anchor Text")
    - .planning/phases/01-docs-seo-aeo/01-CONTEXT.md (D-08, D-11, D-12, D-13)
    - components/ArticlePage.jsx (to confirm where the H1 actually comes from — frontmatter.title, line ~93)
  </read_first>
  <action>
Create a NEW file `docs/AUTHOR-GUIDELINES.md` with the following exact contents. This is the canonical author-facing reference for content rules. It MUST cover D-08 (H1 + slug + updatedAt), D-11 (intros), D-12 (descriptions), and D-13 (link anchors).

```markdown
# OVIAH Help Center — Author Guidelines

These rules exist so docs.oviah.com ranks in Google AND surfaces in AI answer engines (ChatGPT, Perplexity, Google AI Overviews, Copilot). Every rule maps to a specific behavior of those systems.

## Frontmatter (required on every article)

Every `.mdx` file in `content/help/<category>/` MUST have this frontmatter:

```yaml
---
title: Connecting OVIAH Pay
description: To connect Stripe or Square to OVIAH, go to Settings > Payments and click Connect on your provider.
category: Payments
slug: connecting-oviah-pay
readTime: 4 min read
updatedAt: 2026-03-01
---
```

- **`title`** — the human-readable article title. Becomes the `<h1>` AND the browser tab title (via the root layout's `title.template`).
- **`description`** — answer-shaped. See "Meta Descriptions" below. Becomes the `<meta name="description">` and the JSON-LD Article description.
- **`category`** — display label (capitalized, e.g. `Payments`). Used in the breadcrumb and the "Related Articles" data.
- **`slug`** — URL-safe descriptive slug. See "Slugs" below.
- **`readTime`** — display string (e.g. `4 min read`). Cosmetic.
- **`updatedAt`** — ISO-format date `YYYY-MM-DD`. **Required.** Google weights freshness for help content. Update this any time you make a substantive edit.

Optional:

- **`faq`** — array of `{ q, a }` objects. When present, the page renders a `FAQPage` JSON-LD schema, which AI answer engines extract verbatim.

```yaml
faq:
  - q: How do I connect Stripe to OVIAH?
    a: Go to Settings > Payments and click Connect Stripe. You'll be redirected to Stripe to authorize the connection.
  - q: Can I use Square instead of Stripe?
    a: Yes. OVIAH supports both Stripe and Square. Go to Settings > Payments to choose your provider.
```

## H1 — automatic, do NOT add manually

The page `<h1>` is rendered by `components/ArticlePage.jsx` from `frontmatter.title`. **Do not add a `# Heading` line to the MDX body.** Doing so creates two H1s on the page, which hurts SEO.

Section headings inside the body should be `<h2>` (currently rendered via raw JSX `<h2 className="section-heading">` in the existing articles).

## Slugs — descriptive, kebab-case

✅ `connecting-oviah-pay`, `block-time-off`, `client-reminders`
❌ `payments-1`, `article-2`, `untitled`

The slug becomes the URL: `https://docs.oviah.com/help/{category}/{slug}`. Both Google and AI engines use the URL as a relevance signal — descriptive slugs outrank cryptic ones.

## Article Intros — direct answers, not preambles

AI answer engines (especially Perplexity and Google AI Overviews) extract the **first clear sentence after a heading** as their answer snippet. Make that sentence a direct, actionable answer.

✅ Do this:
```md
## How do I set my availability?

Go to **Calendar > Availability** and select the days and hours you accept bookings. Changes save automatically.
```

❌ Not this:
```md
## Setting Your Availability

In this section, we'll walk you through the process of configuring your availability and explain why it's important for your business...
```

The pattern: **action verb + concrete UI path + outcome.** No filler. No "in this guide we will".

## Meta Descriptions — answer-shaped

The `frontmatter.description` field becomes the `<meta name="description">`. Apply the same rule as intros.

✅ `"To set up your booking page in OVIAH, go to Settings > Booking Page and customize your link, hours, and services."`
❌ `"OVIAH's powerful booking tools help beauty pros grow their business."`

Aim for 140-160 characters. Lead with the answer.

## Internal Link Anchor Text — question-matched

Anchor text MUST describe the destination as if completing the user's question.

✅ `"Learn how to set up Stripe payouts in OVIAH."`
❌ `"See this article for more info."` / `"click here"` / `"this page"`

Google and AI engines use anchor text to understand what the linked page is about. Generic anchors waste the signal.

## Updating an article

When you make a substantive edit:

1. Update `frontmatter.updatedAt` to today's date in `YYYY-MM-DD` format.
2. Re-read the first paragraph after each `## heading` — does it still answer-shape? If not, fix.
3. Re-read `frontmatter.description` — still answer-shaped?

## Why this matters

Every rule above is a direct response to how AI answer engines and Google rank/extract help content as of 2026. The rules will evolve; this document will evolve with them. When in doubt, ask: "If a user typed this article's question into ChatGPT, would ChatGPT cite us?"
```

Notes:
- This is a markdown document for humans. No frontmatter, no JSX.
- Length is intentional — it must be self-contained so authors don't need to consult three other docs.
- Do NOT modify `docs/PHASE-docs-seo-aeo.md` — that's the historical spec; this is the living author reference.
  </action>
  <verify>
    <automated>cd /Users/queenb/Desktop/oviah-docs && node -e "const fs=require('fs');const s=fs.readFileSync('docs/AUTHOR-GUIDELINES.md','utf8');const checks=['# OVIAH Help Center — Author Guidelines','## Frontmatter','## H1','## Slugs','## Article Intros','## Meta Descriptions','## Internal Link Anchor Text','answer-shaped','frontmatter.title','updatedAt','faq:','do NOT add manually'];for(const c of checks){if(!s.includes(c)){console.error('MISSING: '+c);process.exit(1);}}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/AUTHOR-GUIDELINES.md` exists
    - File contains a top-level `# OVIAH Help Center — Author Guidelines` heading
    - File contains sections covering: Frontmatter, H1, Slugs, Article Intros, Meta Descriptions, Internal Link Anchor Text
    - File explicitly states the H1 comes from `frontmatter.title` and authors should NOT add a `# Heading` to the MDX body
    - File includes both ✅ and ❌ examples for intros, descriptions, and anchor text
    - File mentions `faq` frontmatter as optional
    - File is at least 80 lines (sanity check that nothing got truncated)
  </acceptance_criteria>
  <done>Author guidelines doc exists at the documented path covering D-08, D-11, D-12, D-13 with examples.</done>
</task>

<task type="auto">
  <name>Task 2: Verify H1 + slug invariants across all 22 articles</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - components/ArticlePage.jsx (lines ~92-94, the rendered H1)
    - .planning/phases/01-docs-seo-aeo/01-CONTEXT.md (D-08)
    - docs/AUTHOR-GUIDELINES.md (just created in Task 1)
  </read_first>
  <action>
This is a verification-only task that closes D-08. It produces no file changes — only a pass/fail signal that the H1 + slug invariants hold. If any article fails the check, STOP and surface to the user — do NOT silently fix.

Run two checks:

1. **Zero `# ` markdown H1s in any MDX body.** The H1 must come from `frontmatter.title` via `ArticlePage.jsx`. Any `# ` line in MDX would render a second H1 (because the page already renders `<h1 className="article-title">{frontmatter.title}</h1>`).

2. **All 22 slugs are descriptive (kebab-case, ≥ 2 words, no numeric suffix).** This is a heuristic check; the planner already eyeballed all 22 and they pass, but re-run it programmatically to catch regressions.

Slug pattern: `^[a-z]+(-[a-z]+)+$` (one or more lowercase words separated by hyphens, at least 2 words).

If both checks pass, this task is done. If either fails, surface the failing files and stop.
  </action>
  <verify>
    <automated>cd /Users/queenb/Desktop/oviah-docs && node -e "const fs=require('fs');const path=require('path');const m=require('gray-matter');const root='content/help';const dirs=fs.readdirSync(root).filter(d=>fs.statSync(path.join(root,d)).isDirectory());let badH1=[],badSlug=[];const slugRe=/^[a-z]+(-[a-z]+)+$/;for(const d of dirs){for(const f of fs.readdirSync(path.join(root,d))){if(!f.endsWith('.mdx'))continue;const p=path.join(root,d,f);const raw=fs.readFileSync(p,'utf8');const parsed=m(raw);if(/^# /m.test(parsed.content))badH1.push(p);const slug=parsed.data.slug||f.replace('.mdx','');if(!slugRe.test(slug))badSlug.push(p+' (slug='+slug+')');}}if(badH1.length){console.error('FAIL: markdown H1 found in:');badH1.forEach(x=>console.error('  '+x));process.exit(1);}if(badSlug.length){console.error('FAIL: non-descriptive slug:');badSlug.forEach(x=>console.error('  '+x));process.exit(1);}console.log('OK — 0 markdown H1s, all 22 slugs descriptive');"</automated>
  </verify>
  <acceptance_criteria>
    - The verification script exits 0
    - The script's stdout contains `OK — 0 markdown H1s, all 22 slugs descriptive`
    - If the script fails, the executor MUST stop and report the failing files to the user (no silent fix)
  </acceptance_criteria>
  <done>D-08 invariants hold: zero markdown H1s in any MDX body, all 22 slugs are descriptive kebab-case.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite all 22 article descriptions and first body paragraphs to answer-shaped form</name>
  <files>content/help/**/*.mdx (all 22 files — see files_modified for the explicit list)</files>
  <read_first>
    - docs/AUTHOR-GUIDELINES.md (just created — the rules being applied)
    - .planning/phases/01-docs-seo-aeo/01-CONTEXT.md (D-11, D-12)
    - docs/PHASE-docs-seo-aeo.md (Sections "Write Article Intros as Direct Answers" and "Meta Descriptions — Answer-Shaped")
    - content/help/payments/connecting-oviah-pay.mdx (representative current state)
    - content/help/getting-started/account-setup.mdx (representative current state)
  </read_first>
  <action>
For EACH of the 22 MDX files, do TWO things:

**A. Rewrite `frontmatter.description`** if it's marketing-shaped (D-12).

The pattern: `[verb/imperative] [object] [in/by going to UI path], [outcome].`

Test each existing description against these rules:
- Does it start with an action verb or "To [verb]..."? (good)
- Does it mention a concrete UI path (Settings > X, Calendar > Y, etc)? (good)
- Is it a marketing tagline like "Powerful X for Y" or "Everything you need..."? (BAD — rewrite)
- Is it under 160 characters? (target — but slightly longer is fine if it's still answer-shaped)

If the existing description already meets the rules (some may), leave it alone. Most will need a rewrite. The planner spot-checked two:

- `connecting-oviah-pay.mdx` current: `"Link your Stripe or Square account to start collecting deposits and getting paid."` → REWRITE to `"To connect Stripe or Square to OVIAH, go to Settings > Payments and click Connect on your provider."`
- `account-setup.mdx` current: `"Everything you need before your first client booking — services, profile, and payments."` → REWRITE to `"To set up your OVIAH account, add your services in Settings > Services, upload a profile photo, and connect a payment provider in Settings > Payments."`

For the other 20, the executor MUST read each file, evaluate, and rewrite where needed. The executor has authority to make editorial decisions per the rules in `docs/AUTHOR-GUIDELINES.md`. Use the existing article's own subject matter — do NOT invent product features.

**B. Rewrite the first body paragraph after the FIRST `<h2>` heading** if it's a marketing/definition preamble (D-11).

The existing articles use raw JSX:
```jsx
<div className="content-section">
<h2 className="section-heading">What is OVIAH Pay?</h2>
<p className="body-text">OVIAH Pay is the built-in payment system that lets you collect deposits...</p>
```

The first `<p className="body-text">` after the first `<h2>` is the AI extraction target. If it's a definition ("X is a tool that...") or a preamble ("In this guide we'll..."), REWRITE it to be a direct answer to the question implied by the H2.

For `connecting-oviah-pay.mdx`:
- H2: "What is OVIAH Pay?" — note this H2 is itself a definition framing. The H2 can stay (it's a valid sub-question) but the FIRST PARAGRAPH should answer that question DIRECTLY in one sentence, then expand. Rewrite the first paragraph to: `"OVIAH Pay is the built-in payment system that lets you connect Stripe or Square through Settings > Payments and start collecting deposits the same day. OVIAH never holds your funds — payouts go directly to your connected account."`
- This is borderline already but adding "Settings > Payments" makes it answer-shaped.

Editorial scope rules:
- Do NOT add new sections, new H2s, new code, or new product claims.
- Do NOT change `relatedArticles` frontmatter or any other frontmatter field except `description` and `updatedAt`.
- DO bump `updatedAt` to `2026-04-13` (today's date per the project context, see CLAUDE memory) on every file you edit, since the content changed.
- Preserve all JSX class names exactly (`<p className="body-text">`, `<div className="content-section">`).
- Preserve the second, third, etc. paragraphs UNLESS they also start with a marketing/preamble pattern. Focus the rewrite on the FIRST paragraph after each section's first `<h2>` — that's the AI extraction target.

Recommended workflow per file:
1. Read the file.
2. Evaluate `description`. Rewrite if needed.
3. Find the first `<p className="body-text">` after the first `<h2 className="section-heading">`. Evaluate. Rewrite if needed.
4. Bump `updatedAt: 2026-03-01` → `updatedAt: 2026-04-13`.
5. Write the file.

Total scope: 22 files, ~3-5 minutes per file = 60-110 minutes Claude execution time. This is a large task but it's editorial sweep work that doesn't touch code structure. If context pressure becomes a concern, the executor MAY split into two batches by category and request user confirmation before continuing.
  </action>
  <verify>
    <automated>cd /Users/queenb/Desktop/oviah-docs && node -e "const fs=require('fs');const path=require('path');const m=require('gray-matter');const root='content/help';const dirs=fs.readdirSync(root).filter(d=>fs.statSync(path.join(root,d)).isDirectory());let total=0,bumped=0,bannedPhrases=[];const banned=[/Everything you need/i,/Powerful .* for/i,/In this section/i,/In this guide/i,/we'll walk you through/i];for(const d of dirs){for(const f of fs.readdirSync(path.join(root,d))){if(!f.endsWith('.mdx'))continue;total++;const p=path.join(root,d,f);const raw=fs.readFileSync(p,'utf8');const parsed=m(raw);const desc=parsed.data.description||'';for(const re of banned){if(re.test(desc))bannedPhrases.push(p+' (description: '+desc+')');}const ua=parsed.data.updatedAt;const uaStr=ua instanceof Date?ua.toISOString().slice(0,10):String(ua);if(uaStr==='2026-04-13')bumped++;}}console.log('total='+total+' bumped='+bumped+' bannedHits='+bannedPhrases.length);if(bannedPhrases.length){bannedPhrases.forEach(x=>console.error('BANNED: '+x));process.exit(1);}if(total!==22||bumped!==22){console.error('expected 22 files all with updatedAt 2026-04-13');process.exit(1);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - All 22 MDX files still exist (no file accidentally deleted)
    - All 22 files have `updatedAt: 2026-04-13` (the rewrite date)
    - Zero descriptions match the banned-phrase regexes (`Everything you need`, `Powerful * for`, `In this section`, `In this guide`, `we'll walk you through`)
    - Spot-check 3 random files: each one's `description` starts with an action verb or "To [verb]..." or contains a concrete UI path (Settings, Calendar, Profile, Booking, etc.)
    - Spot-check the same 3 files: the first `<p className="body-text">` after the first `<h2 className="section-heading">` does NOT start with "In this section", "In this guide", "We'll walk you through", or "Welcome to"
    - All `relatedArticles`, `category`, `slug`, `title`, `readTime` frontmatter fields are unchanged from before this task (verify by sampling 3 files)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 22 articles have answer-shaped descriptions and answer-shaped first body paragraphs; updatedAt bumped to 2026-04-13; no other frontmatter changes; build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of content rewrites</name>
  <what-built>
22 articles had their `description` and first body paragraph (after the first `<h2>`) rewritten to answer-shaped form. The author guideline doc at `docs/AUTHOR-GUIDELINES.md` documents the patterns. The build passes.
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and visit `http://localhost:3000/help/payments/connecting-oviah-pay`.
2. Check the browser tab title — should read `Connecting OVIAH Pay | OVIAH Help Center` (proves Plan 01's `generateMetadata` + `template` are working).
3. View page source. Find the `<meta name="description"` tag. Confirm the description is the rewritten answer-shaped form (mentions a UI path, no marketing filler).
4. View page source. Find the `<script type="application/ld+json">` tag with `"@type":"Article"`. Confirm it exists (Plan 03 wiring).
5. Read the article body in the browser. The first paragraph after "What is OVIAH Pay?" should be the rewritten answer-shaped version.
6. Spot-check 2 more articles of your choice. Same pattern.
7. Visit `http://localhost:3000/sitemap.xml`. Should be valid XML with 23 `<url>` entries.
8. Visit `http://localhost:3000/robots.txt`. Should reference `https://docs.oviah.com/sitemap.xml`.
9. Visit `http://localhost:3000/llms.txt`. Should be plain text starting with `# OVIAH Help Center` and listing all 22 articles as bullets.
10. Visit `http://localhost:3000/llms-full.txt`. Should be plain text with `### {article title}` sections and full body content for each article.

If any rewrite reads awkwardly or invents a feature OVIAH doesn't have, flag it for revision. If everything looks correct, type `approved` to continue.
  </how-to-verify>
  <resume-signal>Type `approved` to continue, or describe specific articles that need revision.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Submit sitemap to Google Search Console + Bing Webmaster Tools</name>
  <what-built>
The site now exposes `https://docs.oviah.com/sitemap.xml` (after deploy). Google Search Console and Bing Webmaster Tools both require human-driven OAuth + property verification + dashboard sitemap upload. Neither has a public API for first-time submission without already-verified ownership. This is the only TRULY-manual step in this phase.
  </what-built>
  <how-to-verify>
**Prerequisite**: This phase's code is deployed to `https://docs.oviah.com` (production). If not yet deployed, this checkpoint waits.

**Google Search Console** (https://search.google.com/search-console):
1. Sign in with the Google account that owns OVIAH's marketing presence.
2. Add a new property if `docs.oviah.com` is not already verified. Use the DNS verification method (add a TXT record at the domain registrar) or the HTML file method (upload to `public/`).
3. Once verified, go to Sitemaps in the left nav.
4. Enter `sitemap.xml` and click Submit.
5. Confirm the status shows "Success" within a few minutes (Google fetches asynchronously).

**Bing Webmaster Tools** (https://www.bing.com/webmasters):
1. Sign in. You can import directly from Google Search Console if you used the same domain there.
2. Verify ownership of `docs.oviah.com` if not already verified.
3. Go to Sitemaps and submit `https://docs.oviah.com/sitemap.xml`.
4. Confirm status shows submitted.

**Perplexity / You.com / Claude / ChatGPT**: No action. Perplexity crawls `llms.txt` automatically; the others discover via sitemap and links over the following days.

When both Google and Bing show the sitemap as accepted, type `submitted` to mark this phase complete.
  </how-to-verify>
  <resume-signal>Type `submitted` once both Google Search Console and Bing Webmaster Tools accept the sitemap. Type `skipped` if deferring to post-launch (and document why in the phase summary).</resume-signal>
</task>

</tasks>

<verification>
1. `docs/AUTHOR-GUIDELINES.md` exists and covers all four content-rule decisions (D-08, D-11, D-12, D-13).
2. The H1/slug verification script (Task 2) reports OK.
3. All 22 MDX files have answer-shaped descriptions (no banned phrases) and `updatedAt: 2026-04-13`.
4. Human checkpoint approved — visual verification of the rewrite quality.
5. Manual sitemap submission completed (or explicitly deferred with reason).
</verification>

<success_criteria>
- Success Criterion 7 (answer-shaped intros + descriptions): satisfied by Tasks 1 + 3 + 4 — guideline doc + 22-file rewrite + human review.
- Closes D-08 (H1/slug) via Task 2 verification + Task 1 documentation.
- Closes D-13 (link anchor guidelines) via Task 1 documentation.
- Closes D-14 (manual index submission) via Task 5 checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/01-docs-seo-aeo/01-05-SUMMARY.md` covering:
- The author guideline doc
- The H1/slug verification result
- A summary table of which articles got their `description` rewritten and which didn't
- A summary table of which articles got their first body paragraph rewritten
- The outcome of the human verification checkpoint
- The outcome of the manual sitemap submission (submitted with timestamp, or deferred with reason)
</output>
</content>
